import 'package:flutter/material.dart';

class SITLSetupScreen extends StatelessWidget {
  const SITLSetupScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SITL Setup'),
      ),
    );
  }
}
