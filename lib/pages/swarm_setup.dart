import 'package:flutter/material.dart';

class SwarmSetupScreen extends StatelessWidget {
  const SwarmSetupScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Swarm Setup'),
      ),
    );
  }
}
